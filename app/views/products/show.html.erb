<div class="container">
  <div class="container pt-5">
    <div class="card-product">
      <% @product.photos.last(1).each do |photo| %>
        <%= cl_image_tag photo.key, crop: :fill %>
      <% end %>
      <div class="card-product-infos">

      <h1><%= @product.name %></h1>
      <h2><%= @product.description %></h2>
      <h3>Vencimiento: <strong><%= @product.expiration_date %></strong></h3>
      <h4> S/. <%= @product.original_price %></h4>
      <h5> S/. <%= @product.price %> </h5>
      <p class="badge rounded-pill text-bg-success h1"> <%= @product.discount.round(2) %>% </p>
      <p class="py-3 pb-3"> <span class="badge rounded-pill text-bg-warning">!
      </span>Este producto puede presentar defectos en su presentación,
      sin embargo su calidad y funcionalidad no se ven afectadas.</p>
      <%= link_to "Agregar", "#", class: "btn btn-ghost" %>
      <%= link_to "Comprar", "#", class: "btn btn-ghost" %>
      </div>
    </div>
  </div>
</div>
